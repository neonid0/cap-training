{
    "welcomeFile": "ui/index-cdn.html",
    "authenticationMethod": "route",
    "logout": {
        "logoutEndpoint": "/logout",
        "logoutPage": "/"
    },
    "routes": [
        {
            "source": "^/ui/(.*)$",
            "target": "$1",
            "destination": "ui",
            "cacheControl": "no-cache, no-store, must-revalidate",
            "authenticationType": "none"
        },
        {
            "source": "^/(.*)$",
            "destination": "srv-api",
            "csrfProtection": true,
            "authenticationType": "xsuaa"
        }
    ]
}
