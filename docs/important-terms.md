Service Provider
Service Handlers
Service Actions

Facades


SELECT (one) from($E, $Id)
INSERT into($E)
UPDATE($E, $Id) set

@cds.search
searchCorpus    : String = make || ' ' || model || ' ' || cast(year as String);
