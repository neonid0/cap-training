{
    "name": "cap-training",
    "version": "1.0.0",
    "dependencies": {
        "@sap/cds": "^9"
    },
    "devDependencies": {
        "@cap-js/sqlite": "^2.4"
    },
    "scripts": {
        "start": "cds-serve"
    },
    "private": true,
    "cds": {
        "query": {
            "limit": {
                "default": 20,
                "max": 100
            }
        }
    }
}
