{
    "name": "cap-training",
    "version": "1.0.0",
    "dependencies": {
        "@sap/cds": "^9"
    },
    "devDependencies": {
        "@cap-js/sqlite": "^2.4",
        "cds-swagger-ui-express": "^0.11.0"
    },
    "scripts": {
        "start": "cds-serve"
    },
    "private": true,
    "cds": {
        "requires": {
            "db": true,
            "messaging": true
        },
        "query": {
            "limit": {
                "default": 20,
                "max": 100
            }
        }
    }
}
